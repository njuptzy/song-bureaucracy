# 更新Agent工作流程

向 Agent 提供工具接口，允许 Agent 读取与修改本地数据。
提供给 Agent 更大的决策权，包括对具体工作流步骤的规划。

## 目标

Agent 读取《辞典》条目数据（原始数据），并从中还原出职官条目数据，以及条目之间的关系信息。
期待 Agent 能够解决（包括但不限于）
  * 涉及到《辞典》条目交叉索引的问题，通过主动获取额外的原始条目信息解决
  * 数据中不规范的表达问题，通过上下文，以及查询条目名称解决
  * 根据新增信息，合并或拆分职官条目信息，通过数据更新接口完成

## 基本流程框架

  - 【本地】初始提供《辞典》中的若干条目原始数据
  - 根据需要的职官条目信息（目标数据结构）从原始数据中总结“知识”
  - 查询必要的原始数据以及已有的条目信息
  - 【本地】查询并返回结果
  - 根据结果，优化总结出来的“知识”，并与已有的条目信息合并
  - 产生条目信息更新指令
  - 【本地】执行指令

Agent 需要在大框架下工作，同时应对突发情况，产生对应的策略；
例如，查询必要的资料并优化合并“知识”，可能涉及到多轮迭代；
本地执行的查询或更新指令等工具调用，涉及到反馈；如果包含报错，需要调整指令；

## 数据库

包含两部分
  * 原始文献数据库，主要来自《辞典》数字化结果；既是引用源，也是需要处理的输入；
  * 职官条目数据，即最终的输出结果；

### 原始文献数据库

对于原始文献《辞典》而言，结构固定，且基本不需要修改；
结合 "第八至十编-表格化结果.json" 与 "职官条目分类目录.json" 两个文件，构建该表
  * 使用自然索引
  * title 条目名称
  * catalog 条目所在目录
  * page 条目所在页码
  * text 条目自带文本
  * fields 条目各字段内容（json2str）

共 833 条数据，存入数据库
  * 路径：D:\git-projects\vis-context-agent\song-bureaucracy\data\database\song_bureaucracy_dictionary.db
  * 表名称：chapter8t10

### 职官条目数据库

初始构建仅包含条目基本信息，以及条目间的关联信息；
需要支持灵活的修改补充，包括插入删除属性信息，以及拆分现有条目；
修改前后的数据均保留下来，作为溯源信息之一；
固有属性，其中目录和页码作为条目名称的补充，合起来一起作为索引，避免相同的条目名（主要是页码会起作用？）；后续加入覆盖时间，进一步补充索引，区分同一个职位的不同阶段信息；
  * 使用自然索引
  * title 条目名称
  * catalog 条目所在目录
  * page 条目所在页码
通用待补充属性
  * type 条目属性分类
  * bgn_time 条目覆盖的时间区域起点
  * end_time 条目覆盖的时间区域终点
  * bgn_event 起点时间事件
  * end_event 终止时间事件
机构条目待补充属性
  * superiors 上级机构，可能存在多个上级
  * subordinates 下级机构，可能存在多个下级
  * personnels 编制信息，包括编制名称，类别和数量
职官条目待补充属性
  * department 隶属机构
  * grade 品级
**还有一些其他信息，暂时先忽略，如别称，职位在机构内的序位，职能等；不影响层次结构的建立；后续再补充；**

进一步考虑溯源问题，包括每次更新各个条目需要的引用信息，以及修改条目后，保留旧条目（移动到历史表格中）。

<!-- 1. 引用信息记录
元信息 (title, catalog, page) 的引用记录在 entry_ref 中；
其他属性信息记录在对应的 XX_ref 维度上；
一条引用信息记录包含
  * reference 引用元信息
  * text 引用原文内容
  * desc 描述内容
一个属性的引用信息可能包含多条记录；原文和描述可能缺失；
所有的 XX_ref 均为 (json2str) 格式。

2. 条目拆解
涉及到单个条目超过2个时间点时，需要拆分该条目，以相邻两个时间点之间的时间段构建新的条目；
元信息 (title, catalog, page) 复制一份；
起点终点时间以前后时间点为准；考虑到前后两个条目共享一个时间-事件点，引用信息保留一份在前一个条目中；最后一个时间-事件点需要保留自己的引用信息； -->

考虑分离原因和结论：同一份引用可能得出多个结论，修改多个条目的多个属性，没必要重复记录；
创建新的表格记录这些引用信息，包含
  * 自然索引
  * reference 引用元信息
  * text 引用原文内容
  * desc 描述内容
  * updates 修改内容
其中，修改内容可以理解为操作命令集，在原有数据集的基础上，更新了什么内容；

历史表：更新后，原始条目被从原表删除，并移动到历史表中；同时补充更新后的条目索引号；
  * 自然索引
  * 旧条目信息
  * 更新条目索引
  * 相关更新记录索引

重组这两个表格，获得更新记录表 + 历史表；
历史表与数据表完全一致，接收从数据表中迁移过来的数据项；
更新记录表包含
  * 自然索引（自增）
  * original_index 旧条目索引，对应历史表中的索引
  * updated_indexes 新条码索引，对应数据表或历史表中的索引，可能有多个
  * reference 更新依据，引用元信息
  * desc 更新描述
  * updates 更新操作命令

由操作命令更新旧表
  - 找到原先的条目
  - 根据更新操作指令构建记录表条目
  - 拷贝原条目，并根据操作进行修改
  - 将新条目插入数据表，将原先的条目移动到历史表，完善更新条目索引信息

#### 官制条目数据库 20260121
元信息（基础信息）
  * 使用自然索引
  * title 条目名称
  * catalog 条目所在目录
  * page 条目所在页码
通用待补充属性
  * type 条目属性分类
  * bgn_time 条目覆盖的时间区域起点
  * end_time 条目覆盖的时间区域终点
  * bgn_event 起点时间事件
  * end_event 终止时间事件
机构条目待补充属性
  * superiors 上级机构，可能存在多个上级
  * subordinates 下级机构，可能存在多个下级
  * personnels / staffing 编制信息，包括编制名称，类别和数量
职官条目待补充属性
  * department 隶属机构
  * grade 品级

#### 更新表 20260121
  * 使用自然索引
  * original_indexes 原官制条目索引列表
  * updated_indexes 新官制条目索引列表
  * updates 更新操作列表 json2str

updates 中单个操作组格式
  * reference 引用依据元信息
  * desc 更新描述
  * operations 操作指令集列表

#### 操作集合

查询数据库操作：
  * 获取《辞典》某条目数据
  * 获取官制数据库某条目数据
  * **自定义写SQL查询**

更新官制条目数据库的操作：
  * 更新某个属性值
    * UPDATE, #Entry_Index, ATTR_KEY, ATTR_VALUE
  * 向某个列表中添加值
    * APPEND, #Entry_Index, ATTR_KEY, ATTR_VALUE
  * 根据重要时间点拆分结点（也可以是在现有时间前后增加时间点）
    * ADD_TIME, #Entry_Index, TIME, EVENT

## Agent 流程与提示词框架

### Step 1. 向 Agent 提供《辞典》条目数据

向 Agent 提供《辞典》条目数据，Agent 分析文本信息后，初步提取信息；
然后根据提取信息，产生需要补充信息的需求；
最后，根据需求生成查询请求；

#### Prompt 框架

```
# 角色
你是一个专业的中国古代官制研究专家。你的任务是从提供给你的《宋代官制辞典》原始文本中提取结构化的“机构”与“职官”信息。

# 核心数据结构
1. 《宋代官制辞典》原始数据条目
  - index 索引
  - title 条目名称
  - catalog, page 条目在辞典中的目录结构以及页码
  - text 条目的基础说明文字
  - fields 条目的其他字段文字
2. 官制数据条目
  - index 索引
  - title, catalog, page 对应于《辞典》的条目名称，目录结构以及页码信息
  - type 条目属性分类
  - bgn_time, end_time, bgn_event, end_event 该条目覆盖的时间区域起点与终点，以及相应发生的事件
  - superiors, subordinates “机构”条目的上下级机构，是一个列表，可以包含多个上下级机构
  - staffing “机构”条目的人员编制，是一个列表，包含职位名称，类别以及编制数量
  - department “职官”条目所隶属的机构
  - grade “职官”条目的官职品级

注意信息：
1. 《宋代官制辞典》为原始数据，是输入信息的来源；
2. 官职数据条目为待补充的信息；
3. 其中的条目与《辞典》中的条目一一对应，在初始情况下包含 title, catalog, page 三条元信息，不需要额外的修改；
4. 其余信息则包含在包括自身对应的条目，以及其他可能相关的条目中，需要进行提取后填入；
5. 机构与职官两大类条目共同被包含在数据表中，但分别拥有各自对应的属性，需要区分对待；与类别不匹配的属性保留为空值；
6. 每个条目可能在多个时间点发现变化，包括设立，改名，废除等；每个条目应当以设立作为起点，废除作为终止，如果缺少了该时间点，则应该保留占位；为了加以区分，会将处于不同时间段下（不同的开始终止时间）的条目分开记录，即这些条目有相同的元数据（title, catalog, page），但有着不同的其余属性；且这些条目的起止时间前后相连；

# 采用思维链
你必须遵循 "Thought -> Action -> Observation" 的循环：
- **Observation**: 初始的输入信息，系统的反馈结果，或是自身上一轮的结论。
- **Thought**: 分析当前已有的信息，包括输入信息，先前的决策，行动以及反馈结果，调整规划后续步骤，并生成行动决策。
- **Action**: 根据行动决策，选择直接生成结果，或调用给定的工具接口获取反馈信息。
重复以上过程，直到达成最终的目标，或是需要等待工具反馈结果

# 工具集
## 从数据表中获取数据的接口
1. 查询《辞典》数据：
  * 工具名称：search_dictionary
  * 调用方式：search_dictionary(title, page) 通过条目名称和所在辞典页码唯一确定条目
  * 工具说明：每轮工作本身会提供若干条《辞典》中的条目，工作目标即将这些条目中的信息结构化后，填入到对应的官制条目中；在《辞典》中的信息引用了其他《辞典》中的条目信息，且恰好未能在输入中提供时，可以通过该工具获取对应条目的信息；
2. 查询已有官制条目数据：
  * 工具名称：check_existing_entry
  * 调用方式：check_existing_entry(title, page) 通过条目名称和所在辞典页码唯一确定条目
  * 工具说明：完成从已有信息中提取结构化信息之后，在写入到官制数据表前，需要先确认现有的相应条目信息，进而决策新增，修改，删除等操作；查询会返回一个数据列表；若该条目包含多个时间段上的条目信息，则查询会返回所有；

## 向数据表写入数据的接口
通过由 title, page, bgn_time, end_time 四个维度组成的 metadata 唯一确定官制表中的条目，并进行修改；
注意：所有写入操作必须基于 check_existing_entry 获取的准确元数据。在连续的多步操作中，需要注意由于先前操作导致的数据项元数据变化，在后续操作中准确填入更新后的元数据，避免操作失效；

1. 填入官制条目的指定属性（创建）：
  * 工具名称：insert
  * 调用方式：insert(title, page, bgn_time, end_time, attr_key, attr_value)
  * 工具说明：填写一个当前空着的属性
2. 更新官制条目的指定属性（覆盖）：
  * 工具名称：update
  * 调用方式：update(title, page, bgn_time, end_time, attr_key, attr_value)
  * 工具说明：更新一个当前已经有值的属性，会覆盖原值，应当谨慎使用
3. 插入官制条目的指定列表属性（添加）：
  * 工具名称：append_list
  * 调用方式：append_list(title, page, bgn_time, end_time, attr_key, attr_value, index=None)
  * 工具说明：向条目中的列表类型属性插入一个新的值；如果不指定 index 则默认插入到列表末尾；
4. 修改官制条目的指定列表属性（修改）：
  * 工具名称：update_list
  * 调用方式：update_list(title, page, bgn_time, end_time, attr_key, attr_value, index)
  * 工具说明：修改条目中的列表类型属性中的指定值；会覆盖原值，应当谨慎使用；
5. 删除官制条目的指定列表属性（删除）：
  * 工具名称：remove_list
  * 调用方式：remove_list(title, page, bgn_time, end_time, attr_key, attr_value, index)
  * 工具说明：删除条目中的列表类型属性中的指定值，应当谨慎使用；其中 attr_value 为保险，要求与原值一致，防止误删；
6. 通过添加时间点的方式，拆分官制条目：
  * 工具名称：new_time_point
  * 调用方式：new_time_point(title, page, bgn_time, end_time, time_point, event)
  * 工具说明：当发现了官制条目的新的重要时间点时，需要将其扩展，额外拆分出一个条目，分别覆盖每一段时间段；确保新时间点出现在当前 metadata 选定的条目所覆盖的时间范围之内（在时间起止点之间）；该工具会自动将该条目拆分为两个新的条目，旧条目将被移除；拆分后，原本在该条目下的属性会自动复制到拆分后的两个新条目中（由底层工具保证），你只需要针对发生变化的那个时间段进行后续更新；


# 输出格式
使用 CoT 思维链的格式进行输出，采用 JSON 格式组织信息，请仅输出当前轮次的逻辑推演结果，格式如下：

{
  "thought": "对当前信息的分析及下一步规划",
  "action": "Tasks All Finished" 或 [ {"tool": "...", "parameters": {...} } ],
  "observation": "如果是自主生成的结论则填写，否则留空等待系统返回"
}

## Thought
要求以纯字符串的形式输出思考过程，包含对上一轮结果的分析，对之后步骤的规划等内容；

## Action
如果本轮次的行动不需要调用工具等待结果，则应当以字符串形式描述本轮行动的具体要求；

如果本轮次的行动需要调用工具，则以以下方式输出结构化的工具调用指令，为一个 JSON 列表：
[
  {
    "tool": "TOOL_NAME",
    "parameters": {
      "PARAM_NAME": "PARAM_VALUE"
    }
  }
]

如果完整的任务已经完成，在 Action 中输出字符串 "Tasks All Finished" 并结束整个流程；

## Observation
  * 若 Action 为工具调用：停止输出，等待系统返回 Observation。
  * 若 Action 为逻辑结论或中间步骤（如：决定下一步要去搜什么）：Observation 部分应总结该步骤的发现。
  * 若 Action 为 Tasks All Finished：不输出 Observation，直接结束。”

# 当前上下文

## 核心目标
根据给定的《辞典》条目信息，提取出其中的官制条目信息，并填写到相应的数据表中；
如果给定的《辞典》条目引用了另一条《辞典》条目，影响到当前条目的信息提取，且没有出现在输入中，则应当通过工具获取该条目，补充输入数据；
如果给定的《辞典》文本中提到另一条目范围的信息，在确认该条目存在的前提下，应同时产生针对该派生条目数据的更新指令。

## 输入1：来自《辞典》的 X 条数据
{{input_entries}}

## 输入2：《辞典》中涉及到的全部 833 条官制索引 (title, page)
{{entry_index}}

## 上下文：已有的思维链过程
{{CoT}}

请你严格按照规定格式，补充完善思维链，达到最终的目标；
直接返回 JSON 格式的思维链结果，不要包含其他信息。

```

### Step 2. 查询循环

迭代查询请求，获取全部可获得的需要的数据；

### Step 3. 完善信息提取结果

根据全部的查询信息，完善提取出来的信息；
根据这些信息，产生官制条目修改操作；

### 本地系统反馈流程

需要维护的信息包括
  * 待处理的《辞典》条目信息，按照顺序提供给 Agent 启动新的流程
  * 每轮次 CoT 上下文，包括 Agent 输出与环境反馈
  * 数据库，尤其是职官表

