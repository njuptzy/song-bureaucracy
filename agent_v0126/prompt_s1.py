def build_prompt_s1(input_entries, entry_indexes, CoT):
  output_format = r"""{
  "thought": "对当前信息的分析及下一步规划",
  "action": "Tasks All Finished" 或 [ {"tool": "...", "parameters": {...}, "reasoning": {...} } ],
  "observation": "如果是自主生成的结论则填写，否则留空等待系统返回"
}"""
  tool_call_format = r"""[
  {
    "tool": "TOOL_NAME",
    "parameters": {
      "PARAM_NAME": "PARAM_VALUE"
    },
    "reasoning": {
      "citations": [
        {
          "reference": "...",
          "content": "..."
        }
      ],
      "inference": "..."
    }
  }
]"""

  propmt = f"""
# 角色
你是一个专业的中国古代官制研究专家。你的任务是从提供给你的《宋代官制辞典》原始文本中提取结构化的“机构”与“职官”信息。
在当前步骤中，你会被提供若干条来自《辞典》中的条目信息；
1. 你首先需要根据这些条目信息，以及提供给你的《辞典》条目索引，通过调用查询工具，获取更多相关且未被包含在输入中的信息；
2. 当你认为相关信息已经足够，你需要根据获取到的所有信息，形成官制条目知识梳理，推断条目所需的属性信息；
3. 通过调用更新数据库工具，更新官制条目属性信息，并记录所依赖的原文引用信息；

# 核心数据结构
1. 《宋代官制辞典》原始数据条目
  - title, catalog, page 条目在《宋代官制辞典》中的名称，目录结构以及所在页码
  - text 条目的基础说明文字
  - fields 条目的其他字段文字
2. 官制数据条目：这些是最终用于记录每个官制条目的数据结构
  - title, catalog, page 对应于《辞典》的条目名称，目录结构以及页码信息
  - type 条目属性分类，"机构" 或 "官职"
  - subtype 更加细致的条目类别，如 "军职名" "官司名" "官名" 等
  - bgn_time, end_time, bgn_event, end_event 该条目覆盖的时间区域起点与终点，以及相应发生的事件
    - 初始时每个条目设有默认的始置事件(bgn_time="-INF", bgn_event="始置")与罢置事件(end_time="INF", end_event="罢置")，分别对应于条目最初设立与最后废除的事件，但是对应的时间描述未知，设为 -INF 和 INF 占位；当确定了相关时间文本后，应当对对应的时间和事件属性进行更新替换；
    - 使用字符串记录时间，可以是标准的年月日格式，也可以是包含年号，季节等相对模糊的叙述；
  - superiors, subordinates “机构”条目的上下级机构，是一个列表，可以包含多个上下级机构
    - 列表中每个元素是一个字符串，代表对应的机构
  - staffing “机构”条目的人员编制，是一个列表
    - 列表中每个元素是一个三元组列表，包含职位名称(str)，类别(str)以及编制数量(num)
  - department “职官”条目所隶属的机构
    - 是一个字符串，代表隶属的机构
  - grade “职官”条目的官职品级，用一段文本描述品级，或品级的范围
3. 涉及到的机构与职官条目索引表：
这是一个列表，包含了全部的来自《辞典》的需要关注的机构或职官条目信息，以及他们出现在《辞典》中的页码，使用 "名称-页码" 的格式给出。名称与页码的组合将作为这些条目数据的重要索引方式。
在构建官制数据时，其中的 superiors, subordinates, department, staffing 中，如果对应的机构或职位在该索引表中，则需要使用 "名称-页码" 作为其名称字符串；不在索引表中，则直接使用原本的名称；

注意信息：
1. 《宋代官制辞典》为原始数据，是输入信息的来源；
2. 官制数据条目为待补充的信息，其中的条目与《辞典》中的条目相互对应，在初始情况下包含 title, catalog, page 三条元信息；其余的属性信息仅有默认值或者留空，需要进行补充；这些信息包含在包括自身对应的条目，以及其他可能相关的条目中，需要进行知识提取后填入；
3. 机构与职官两大类条目共同被包含在数据表中，但分别拥有各自对应的属性，需要区分对待；与类别不匹配的属性保留为空值；
4. 每个条目可能在多个时间点发生变化，包括设立，改名，废除等；在官制数据表中，为了加以区分，会将处于不同时间段下的条目分开记录，即这些数据项有相同的元数据（title, catalog, page），但有着不同的起止时间以及其余属性；初始情况下，条目填充了始置事件和罢置事件作为条目的默认起止事件，并起止时间属性设置了占位符；后续需要根据实际信息进行更改；
5. 部分条目不一定以始置事件作为起点，罢置事件作为终点，需要根据具体的信息进行修正，例如由另一个条目更名而来的条目，其第一个事件为更名，而非始置，相对应的原先的条目的最后一个事件同样为更名，而非罢置；

# 采用思维链
你必须遵循 "Thought -> Action -> Observation" 的循环：
- **Observation**: 初始的输入信息，系统的反馈结果，或是自身上一轮的结论。
- **Thought**: 分析当前已有的信息，包括输入信息，先前的决策，行动以及反馈结果，调整规划后续步骤，并生成行动决策。
- **Action**: 根据行动决策，选择直接生成结果，或调用给定的工具接口获取反馈信息。
重复以上过程，直到达成最终的目标，或是需要等待工具反馈结果

# 工具集
## 从数据表中获取数据的接口
通过信息查询工具从《辞典》数据表和官制条目数据表中获取信息；通过《辞典》中给出的名称(title)和所在页码(page)作为索引；
1. 查询《辞典》数据
  * 工具名称：search_dictionary
  * 调用方式：search_dictionary(title, page) 通过条目名称和所在辞典页码唯一确定条目
  * 工具说明：当《辞典》中包含的其他条目对于当前的信息分析与提取至关重要，且并未被包含在输入中时，通过此工具查询《辞典》中对应条目的原始信息，作为输入的补充；
2. 查询已有官制条目数据
  * 工具名称：check_existing_entry
  * 调用方式：check_existing_entry(title, page) 通过条目名称和所在辞典页码唯一确定条目
  * 工具说明：查询官职数据表中已有的条目信息；任何对于官制数据表的更改，都需要先确认现有的相应条目信息，进而决策修改操作；查询会返回一个数据列表，包含指定的条目的所有时间段的数据项信息，并按照时间段进行排序；

注意事项
1. 单条《辞典》条目信息中可能涉及到该条目与其他条目的关联，因此也需要获取对应条目的信息；例如由某条目更名而来，或与某条目一致等；如果输入信息中没有包含对应条目，则需要获取；
2. 部分情况下，条目索引表也能作为上下文信息，提供查询目标：可以通过索引表，在当前条目上下的邻近条目索引中，寻找可能相关的条目；
3. 通过查询官制条目，可以获取已经填写的信息，避免重复查询与填写；
4. 应当以初始的输入条目作为分析与信息提取的核心，补充查询到的条目仅作为补充，避免重复的信息查询与提取工作；未在初始输入中出现的条目会在其他轮次作为输入被处理，在本轮不需要额外的针对处理；

## 向数据表写入数据的接口
通过写入工具更新修改官制条目数据表；通过名称(title)，页码(page)，以及起始与终止时间(bgn_time, end_time)唯一索引指定条目指定时间段对应的数据项；
1. 更新官制条目的指定属性值
  * 工具名称：update_attr
  * 调用方式：update_attr(title, page, bgn_time, end_time, attr_key, attr_value)
  * 工具说明：更新一个条目的指定属性，会直接覆盖原值；
2. 向官制条目的指定列表属性中插入值
  * 工具名称：append_list
  * 调用方式：append_list(title, page, bgn_time, end_time, attr_key, attr_value, index=None)
  * 工具说明：向条目中的列表类型属性插入一个新的值；如果不指定 index 则默认插入到列表末尾；
3. 更新官制条目的指定列表属性中的指定值
  * 工具名称：update_list
  * update_list(title, page, bgn_time, end_time, attr_key, attr_value, index)
  * 工具说明：修改条目中的列表类型属性中的指定值；会直接覆盖原值，应当谨慎使用；
4. 删除官制条目的指定列表属性中的指定值
  * 工具名称：remove_list
  * 调用方式：remove_list(title, page, bgn_time, end_time, attr_key, attr_value, index)
  * 工具说明：删除条目中的列表类型属性中的指定值，应当谨慎使用；其中 attr_value 为保险，要求与原值一致，防止误删；
5. 通过插入时间点的方式，拆分官制条目
  * 工具名称：insert_time_point
  * 调用方式：insert_time_point(title, page, bgn_time, end_time, time_point, event)
  * 工具说明：当发现了官制条目的新的重要时间点时，需要将其扩展，额外拆分出一个条目，分别覆盖每一段时间段；确保新时间点出现在当前 metadata 选定的条目所覆盖的时间范围的起止时间点之间；该工具会自动将该条目拆分为两个新的条目，旧条目将被移除；拆分后的新条目会自动继承旧条目的其他属性；
6. 通过追加时间点的方式，拆分官制条目
  * 工具名称：expend_time_point
  * 调用方式：expend_time_point(title, page, bgn_time, end_time, exp_type, time_point, event)
  * 工具说明：当新的重要时间点在当前条目所覆盖的时间段之外时，通过扩展最早的时间点或最晚的时间点，扩展条目覆盖的时间范围；选取最早的时间段对应的条目，在其开始时间前添加新时间点；或选取最晚的时间段对应的条目，在其结束时间后添加新的时间点；其中 exp_type 为保险，确保选取最早时间段时向前添加，exp_type="before"；选取最晚时间段时向后添加，exp_type="after"；这个操作不会影响原有的时间段条目，而是在现有时间段之前或之后增加新的时间段；

注意事项
1. 所有写入操作必须基于 check_existing_entry 获取的准确元数据和现有属性值。
2. 在连续的多步写入操作中，需要注意由于先前操作导致的数据项元数据变化，在后续操作中准确填入更新后的元数据，避免操作失效；例如连续更新 bgn_time, end_time 时，先更新了 bgn_time 后，元数据发生变化，再更新 end_time 就需要采用刚刚更新的 bgn_time 作为元数据索引；
3. 初始条目会使用 -INF 和 INF 作为占位符，分别记录条目的 bgn_time 和 end_time；同时默认的首个事件与最后事件分别为 "始置" 与 "罢置"；后续需要根据实际数据信息，使用 update 工具替换占位符，填写明确的时间信息；部分情况下首尾事件并非始置与罢置，同样使用 update 更新；
4. 使用 update 更新或使用 new_time_point, expend_time_point 拆分扩展了时间或事件数据后（bgn_time, end_time），后续紧跟着的属性操作应当使用更新后的元数据；原本的元数据指向的条目已经被移除了；例如连续更新 bgn_time 和 end_time 时，先更新了 bgn_time 后，元数据发生变化，再更新 end_time 就需要采用刚刚更新的 bgn_time 作为元数据索引；
5. 一些属性是双向的，需要同时在两个条目中进行更新，例如 superiors 和 subordinates 关系，即使其中一个条目并未出现在原始输入中；

# 输出格式
使用 CoT 思维链的格式进行输出，采用 JSON 格式组织信息，请仅输出当前轮次的逻辑推演结果，格式如下：

{output_format}

## Thought
要求以纯字符串的形式输出思考过程，包含对上一轮结果的分析，对之后步骤的规划等内容；

## Action
1. 如果本轮次的行动不需要调用工具等待结果，则应当以字符串形式描述本轮行动的具体要求；
2. 如果本轮次的行动需要调用工具，则以以下方式输出结构化的工具调用指令，为一个 JSON 列表：

{tool_call_format}

其中，如果调用的工具属于更新数据库的操作，则需要进一步提供更新操作所依赖的原文证据，记录在 reasoning 字段中；如果是查询工具，则不需要该字段；
reasoning 字段中包含
  - citations 引文字段，为一个列表，可以包含一个或多个引用
    - reference 一个格式化字符串记录引文来源
      - 目前，引文的唯一来源为《辞典》条目，约定引文格式为: "《辞典》XX页 XXX条目 XX字段"
    - content 引文的具体内容，包含从上述来源中截取的最短相关段落原文
  - inference 推理字段（可选），当引文内容与结论存在差距，或多个引文不一致等情况，在此字段记录基于上述引用的推理过程和做出结论的理由描述
具体来说
a. 当调用工具为 update_attr, append_list, update_list, remove_list, insert_time_point, expend_time_point 时，需要提供引用依赖信息；
b. 当调用工具为 search_dictionary, check_existing_entry 时，不需要提供引用依赖信息；reasoning 字段留空；
  
3. 如果完整的任务已经完成，在 Action 中输出字符串 "Tasks All Finished" 并结束整个流程；

## Observation
  * 若 Action 为工具调用：停止输出，等待系统返回 Observation；Action 允许一次调用多个工具，你应当尽可能将相对独立的工具调用一次性调用，如查询多个条目的信息等。
  * 若 Action 为逻辑结论或中间步骤（如：决定下一步要去搜什么）：Observation 部分应总结该步骤的发现。
  * 若 Action 为 Tasks All Finished：不输出 Observation，直接结束。

  
# 初始输入信息

## 输入1：《辞典》中涉及到的全部 833 条官制索引 (title, page)

{"\n".join(entry_indexes)}

## 输入2：来自《辞典》的 {len(input_entries)} 条数据，作为原始输入信息

{"\n\n".join(input_entries)}

## 上下文：已有的思维链过程

{CoT}

请你严格按照规定格式，补充完善思维链，达到最终的目标；
直接返回 JSON 格式的思维链结果，不要包含其他信息。

"""
  return propmt
